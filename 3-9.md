# 2026-03-09

### IGNORE
- separate python package (later)

class CircleView(EllipseView):
def **init**(
self, x: float, y: float, radius: float,
fill: int = Display.OFF, outline: Optional[int] = Display.ON,
stroke_width: int = 1
) -> None:
super().**init**(
x - radius, y - radius,
radius_ 2, radius _2,
fill=fill, outline=outline,
stroke_width=stroke_width
)

    @property
    def radius(self) -> int:
        return self.width / 2

# Work on Record, Playback, and both simulataneously
- pyaudio
- save to wav folder
- add a TableViewController to select different input file
- mic test view, if 1-4 mics are plugged in, show their output levels

~454 Mb is 1 hour WAV


display how much time (in hours)(space, internally) is remaining
depending on the number of mics

- recording indefinetely without playing
    - record accross all mics
- record for length of sound file
    - select which mic to record from




